.works {
    padding: 200px 0;

    @include tablets {
        padding: 115px 0;
    }
}

.works__title {
    margin-bottom: 83px;
    
    @include tablets {
        margin-bottom: 48px;
    }
}

.works__preview {
    display: flex;
    justify-content: space-between;

    @include tablets {
        flex-direction: column;
        align-items: center;
    }
}

.works-preview__pictures {
    position: relative;
    width: 66%;
    z-index: 10;
    height: 34vw;
    margin-right: 9vw;


    &:before {
        content: attr(data-slide);
        position: absolute;
        top: 14%;
        right: 0;
        color: #E6E6E6;
        line-height: 1;
        font-size: 22.5vw;
        font-weight: 800;
        transform: translateX(9vw);

        @include desktop {
            top: 15%;
            font-size: 26vw;
        }

        @include tablets {
            font-size: 37vw;
            transform: translateX(15vw);
        }

        @include phones {
            display: none;
        }
    }

    @include desktop {
        height: 34vw;
    }

    @include tablets {
        margin-bottom: 56px;
        margin-right: 0;
        height: 39vw;
    }

    @include phones {
        width: 100vw;
        margin-right: 0;
        height: 58.2vw;
    }
}

.works-preview__pictures-content {
    z-index: 10;
    position: absolute;
    top: 0;
    right: 0;
    width: 100%;
    height: 100%; 
}

.works-preview__img {
    position: relative;
    z-index: $z-slider;
}

.works-preview__thumbs {
    position: absolute;
    bottom: 10%;
    right: 0;
    z-index: $z-slider-btn;

    @include phones {
        display: none;
    }
}

.works-preview__btns {
    position: absolute;
    bottom: 10%;
    right: -53px;
    z-index: $z-slider-btn;

    @include desktop  {
        right: -34px;
    }

    @include phones {
        right: 0;
        bottom: 0;
        z-index: $z-slider-btn;
    }
}

.works-preview__info {
    z-index: $z-slider;
    width: 325px;
    flex-shrink: 0;
    color: #333C49;

    @include desktop {
        width: 300px;
    }

    @include tablets {
        width: 422px;
        text-align: center;
        display: flex;
        flex-direction: column;
        align-items: center;
    }

    @include phones {
        width: 100%;
        text-align: left;
        align-items: flex-start;
    }
}

.works-preview__title {
    margin: 35px 0;
    font-weight: bold;
    font-size: 30px;
    line-height: 41px;
}

.works-preview__desc {
    font-weight: 600;
    font-size: 16px;
    line-height: 30px;
    margin-bottom: 46px;
}

.works-preview__fullpic-container {
  position: absolute;
  top: 0;
  right: 0;
  width: 100%;
  height: 100%; 
}

.fade-enter-active {
    animation: enterSlider 1s;
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
}

.fade-leave-active {
    animation: leaveSlider 1s;
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
}

@keyframes enterSlider {
    0% {
        transform: translateX(-1000px);
        opacity: 0;
    }

    39% {
        opacity: 1;
    }

    60% {
        transform: translateX(100px);
    }

    100% {
        transform: translateX(0px);
        opacity: 1;
    }
}

@keyframes leaveSlider {
    39% {
        opacity: 1;
    }
    
    40% {
        opacity: 0;
    }

    100% {
        opacity: 0;
    }
}
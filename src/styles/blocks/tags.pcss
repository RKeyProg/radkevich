.tags {
    flex-wrap: wrap;
}

.tags__item {
    padding: 14px 22px;
    background: $tags-bacground-color;
    border-radius: 40px;
    margin: 0 8px 8px 0;

    &:last-child {
        margin-right: 0;
    }

    @include phones {
        padding: 10px 20px;
    }
}